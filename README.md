# TheCatAPI-Example
An example of a service based architecture using TheCatAPI.

In order to run this project you will need to add an `xcconfig` file called `Secrets` to the top directory with your `thecatapi` key as the value of `CAT_API_KEY`. An API key can be obtained from https://thecatapi.com/.
